<%= form_with(model: take_inventory, local: true) do |form| %>
  <% if take_inventory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(take_inventory.errors.count, "error") %> prohibited this take_inventory from being saved:</h2>

      <ul>
      <% take_inventory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fiscal_year %>
    <%= form.date_select :fiscal_year %>
  </div>

  <div class="field">
    <%= form.label :beginning_product_inventory %>
    <%= form.number_field :beginning_product_inventory %>
  </div>

  <div class="field">
    <%= form.label :cost_of_sales %>
    <%= form.number_field :cost_of_sales %>
  </div>

  <div class="field">
    <%= form.label :period_end_product_inventory %>
    <%= form.number_field :period_end_product_inventory %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
